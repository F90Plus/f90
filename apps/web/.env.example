# ─────────────────────────────────────────────────────────────────────────────
# F90+ — Environment template
# Copy to `.env.local` and fill in. NEVER commit `.env.local` (it is gitignored).
#
# The foundation phase needs NONE of these. They are placeholders that document
# the integrations planned on the roadmap so the wiring is obvious later.
# ─────────────────────────────────────────────────────────────────────────────

# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# ── Supabase (later: auth, profiles, virtual wallet, leaderboards) ───────────
# NEXT_PUBLIC_SUPABASE_URL=""
# NEXT_PUBLIC_SUPABASE_ANON_KEY=""
# SUPABASE_SERVICE_ROLE_KEY=""

# ── Football data (later: live fixtures, scores, standings) ──────────────────
# FOOTBALL_API_BASE_URL=""
# FOOTBALL_API_KEY=""

# ── AI pundit (later: rule/stats-based predictions engine) ───────────────────
# AI_PROVIDER=""
# AI_API_KEY=""
