# Resolve peer deps automatically (React 19 / Next 16 bleeding-edge friendliness)
auto-install-peers=true
strict-peer-dependencies=false

# Keep the lockfile honest in CI
prefer-workspace-packages=true
